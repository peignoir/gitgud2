{
  "buildCommand": "npm run build:mobile",
  "outputDirectory": "dist-client",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://YOUR-API-DOMAIN.example.com/api/$1"
    }
  ]
}

